 Gemini hay un asunto medianamente complicado que voy a tener que ceder frente al usuario. El asunto es el siguiente. Como él no tiene un sistema, obviamente se le estoy construyendo, él utiliza la siguiente lógica para la generación de números de contrato. Si un inversionista se retira, él recicla el número de contrato. Por ejemplo, si el inversionista con el contrato uno del fondo pime uno se fue y, digamos, ya estamos en el inversionista 100, no genera un 101, sino vuelve al uno. Es una lógica entreberada, pero es un lógico. Mi lógica, la que tú vas a revisar en el sistema, es de generar un correlativo. Y el contrato nace, vive y cierra con el mismo número y ese número no se vuelve a tocar más. Entonces, lo que quiero antes de aplicar el control de calidad es que tú tengas, en claro, que vamos a tener que incorporar su lógica. Es decir, empezamos con una base de datos donde cada contrato tiene un número. Y luego, esos contratos obviamente están vigentes. Si el contrato se acaba, entonces lo que pasaría, digamos, a validar nuevamente el número, estar disponible para otro contrato. Entonces, los contratos que están en condición cerrado tendrían un número que volvería a ser viable para un nuevo contrato. Tenemos que comparar las lógicas, ¿por qué? ¿Por qué para diferenciarlos? O sea, si tenemos un contrato cerrado con un número extinto, tendríamos que añadir algún sufijo al número. Por ejemplo, el contrato uno generado el primero de enero del 2023 y tenemos el contrato uno generado el primero de enero de el 2016. Son diferentes contratos, solamente que comparten el número uno del fondo uno. Entonces, vamos a imponernos bien en claro esa lógica de los... No sé cómo llamarla, ¿no? de la numeración, no correlativa de contratos para poder satisfacer el user que se resiste a utilizar algo más lógico.