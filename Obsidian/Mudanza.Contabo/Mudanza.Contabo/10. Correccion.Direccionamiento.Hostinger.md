# 10. Corrección de Direccionamiento Residual hacia Hostinger & Independencia de Contabo

---

## 📌 1. Resumen Ejecutivo y Diagnóstico Forense

Durante la auditoría de salud del VPS de Producción de **Hostinger** (`91.108.125.253` / VPS de PETRAL), se detectó que el servicio legacy de **ERP InAndes** (`inandes-api.service`, `run_fastapi.py`) seguía consumiendo **31.1% de memoria RAM (~1.25 GB)** y continuaba recibiendo llamadas activas de generación de reportes y PDFs:
- **Última llamada registrada**: `24 de Agosto a las 11:39 AM (16:39 UTC)` desde IPs de operadoras locales (`38.25.30.48` / `190.234.182.191`).
- **Endpoints llamados**: `POST /api/inversionistas/generate-pdf`, `GET /api/inversionistas/eecc/...`, `GET /api/inversionistas/retenciones/...`.

---

## 🔍 2. Auditoría Integral: Las 4 Causas Raíz Identificadas

A pesar de que el frontend principal `https://inandes.geeksoft.tech/` ya había sido migrado a **Contabo** (`169.58.168.107` con Traefik/Coolify), existían 4 factores encadenados que mantenían vivo el tráfico hacia Hostinger:

### Causa A: El Registro DNS `api-factoring.geeksoft.tech` nunca fue migrado
Al realizar la resolución de nombres DNS (`Resolve-DnsName`):
- `inandes.geeksoft.tech` ➔ `169.58.168.107` (**Contabo** - Frontend React 19 / Traefik Proxy).
- `api-factoring.geeksoft.tech` ➔ `91.108.125.253` (**Hostinger** - Nginx / FastAPI Legacy).

Cualquier servicio o script que invocara el dominio oficial de la API seguía aterrizando en el servidor antiguo de Hostinger.

### Causa B: URLs Absolutas Hardcodeadas en Componentes Frontend
Se identificaron 4 puntos en React con URLs absolutas hacia el subdominio legacy de Hostinger (`https://inandes.react.geeksoft.tech`):

| Archivo | Línea Original | Código Anterior (❌ Hostinger Fijo) | Código Corregido (✅ Dinámico Contabo) |
| :--- | :--- | :--- | :--- |
| `src/features/fondos/FondosPage.tsx` | L288 | `fetch('https://inandes.react.geeksoft.tech/api/inversionistas/generate-pdf', ...)` | `fetch(`${getApiBaseUrl()}/api/inversionistas/generate-pdf`, ...)` |
| `src/features/inversionistas/InversionistasPage.tsx` | L127 | `fetch('https://inandes.react.geeksoft.tech/api/inversionistas/generate-pdf', ...)` | `fetch(`${getApiBaseUrl()}/api/inversionistas/generate-pdf`, ...)` |
| `src/features/inversionistas/InversionistasPage.tsx` | L2668 | `window.open('https://inandes.react.geeksoft.tech/api/inversionistas/eecc/...', '_blank')` | `window.open(`${getApiBaseUrl()}/api/inversionistas/eecc/...`, '_blank')` |
| `src/features/inversionistas/InversionistasPage.tsx` | L2724 | `window.open('https://inandes.react.geeksoft.tech/api/inversionistas/retenciones/...', '_blank')` | `window.open(`${getApiBaseUrl()}/api/inversionistas/retenciones/...`, '_blank')` |

### Causa C: Función `getApiBaseUrl()` duplicada y desalineada en `factoringService.ts`
En `src/services/factoringService.ts`, existía una copia privada de `getApiBaseUrl()` que devolvía `'https://api-factoring.geeksoft.tech'` (Hostinger) en lugar de utilizar la configuración centralizada de `src/config/apiConfig.ts` (que devuelve rutas relativas `""` en producción para que las atienda el reverse proxy de Contabo).

### Causa D: Estado del Reverse Proxy en Contabo (`HTTP 502 Bad Gateway`)
Al auditar los endpoints `/api/*` directamente contra el VPS de Contabo:
- `https://inandes.geeksoft.tech/` ➔ **HTTP 200 OK** (Traefik entrega React SPA).
- `https://inandes.geeksoft.tech/api/docs` ➔ **HTTP 502 Bad Gateway** (Traefik).
- `https://inandes.geeksoft.tech/api/health` ➔ **HTTP 502 Bad Gateway** (Traefik).

---

## 🛠️ 3. Modificaciones Aplicadas en el Código Fuente

Se unificó el consumo de endpoints mediante el helper centralizado `getApiBaseUrl()` de `src/config/apiConfig.ts`:
- **En Entorno de Producción (Contabo)**: Retorna `""` (ruta relativa), permitiendo que todas las peticiones `/api/...` sean resueltas directamente por el Reverse Proxy de Contabo (Traefik/Coolify) hacia el contenedor de backend correspondiente.
- **En Entorno Local (Desarrollo)**: Retorna `http://localhost:8000` (o el valor definido en `VITE_API_FACTORING_URL`).

### Diff de Cambios Aplicados:

```diff
// FondosPage.tsx & InversionistasPage.tsx
+ import { getApiBaseUrl } from '../../config/apiConfig';

- const response = await fetch('https://inandes.react.geeksoft.tech/api/inversionistas/generate-pdf', {
+ const API_BASE = getApiBaseUrl();
+ const response = await fetch(`${API_BASE}/api/inversionistas/generate-pdf`, {
```

```diff
// InversionistasPage.tsx (Pestañas de EECC y Retenciones)
- window.open(`https://inandes.react.geeksoft.tech/api/inversionistas/eecc/${fondo}/${fEnd}`, '_blank');
+ const API_BASE = getApiBaseUrl();
+ window.open(`${API_BASE}/api/inversionistas/eecc/${fondo}/${fEnd}`, '_blank');

- window.open(`https://inandes.react.geeksoft.tech/api/inversionistas/retenciones/${fondo}/${fEnd}`, '_blank');
+ const API_BASE = getApiBaseUrl();
+ window.open(`${API_BASE}/api/inversionistas/retenciones/${fondo}/${fEnd}`, '_blank');
```

```diff
// factoringService.ts
+ import { getApiBaseUrl } from '../config/apiConfig';
- const getApiBaseUrl = (): string => { ... return 'https://api-factoring.geeksoft.tech'; };
```

---

## 🏁 4. Estado de Desactivación en Hostinger (Completado)

- [x] **Apagado en Hostinger (`91.108.125.253`)**: **COMPLETADO AYER**.
  - `systemctl stop inandes-api inandes-backend erp_inandes`
  - `systemctl disable inandes-api inandes-backend erp_inandes`
  - Procesos residuales terminados. Memoria RAM liberada para PETRAL (~1.4 GB).

---

## 🚀 5. Plan Definitivo de 3 Pasos para Independencia Total en Contabo

| Paso | Acción Requerida | ¿Quién lo ejecuta? | Estado |
| :---: | :--- | :---: | :---: |
| **Paso 1** | **Levantar y verificar Backend FastAPI en Contabo** (`inandes-api`) para resolver el `502 Bad Gateway` en `/api/*`. | 🤖 **Agente AI** (vía SSH a Contabo `169.58.168.107`) | **En Curso** |
| **Paso 2** | **Conmutación DNS**: Cambiar registro `A` de `api-factoring.geeksoft.tech` a `169.58.168.107` en el panel DNS. | 👤 **Usuario** (en Cloudflare / Registrador DNS) | **Pendiente Usuario** |
| **Paso 3** | **Desplegar Frontend React Limpio**: `git add .`, `git commit`, `git push origin main` para auto-deploy en Coolify. | 🤖 **Agente AI** (vía Git local) | **Listo para Push** |

---

### Detalle de los Pasos:

#### 🔹 Paso 1 (Agente AI - Vía SSH a Contabo):
1. Conectarse al VPS de Contabo (`169.58.168.107`) vía SSH.
2. Iniciar / Desplegar la aplicación backend `3g5kcala3ypqzlsrhyelxyev` en Coolify.
3. Verificar que responda `HTTP 200 OK` en `http://localhost:8000/api/docs` o `https://inandes.geeksoft.tech/api/docs`.

#### 🔹 Paso 2 (Usuario - Panel DNS):
1. Ingresar a su panel de gestión DNS (Cloudflare o proveedor de dominio `geeksoft.tech`).
2. Localizar el registro tipo **`A`** para **`api-factoring`** (o `api-factoring.geeksoft.tech`).
3. Modificar la IP de destino:
   - **IP Anterior (Hostinger)**: `91.108.125.253`
   - **IP Nueva (Contabo)**: **`169.58.168.107`**
4. Guardar cambios. *(La propagación suele tomar entre 1 y 5 minutos)*.

#### 🔹 Paso 3 (Agente AI - Git & Coolify Auto-deploy):
1. Comitear los archivos modificados (`FondosPage.tsx`, `InversionistasPage.tsx`, `factoringService.ts`, y las notas de Obsidian).
2. Ejecutar `git push origin main`.
3. El webhook de Coolify construirá y publicará la versión oficial de React 19 sin referencias a Hostinger.

---

## 🎯 6. Beneficios Obtenidos
1. **Liberación de Memoria en Hostinger**: Se recuperaron **~1.4 GB de RAM** (36.5% del servidor para PETRAL).
2. **Eliminación de Bucles de CPU**: Se canceló el loop de reinicios fallidos de `inandes-backend.service`.
3. **Independencia Total**: Contabo opera como servidor autosuficiente, autónomo y de alta velocidad sin dependencias cruzadas.

---
*Documento oficial del plan de migración e independencia cloud.*
