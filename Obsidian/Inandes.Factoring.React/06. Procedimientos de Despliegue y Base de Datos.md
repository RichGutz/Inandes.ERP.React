# Procedimientos de Despliegue y Base de Datos

Esta nota sirve como referencia permanente para los Agentes IA (y humanos) sobre cómo operar, desplegar y conectar los servicios del ecosistema ERP Factoring & Inversionistas INANDES.

---

## 1. Repositorio Oficial de Git

El repositorio principal, oficial e inequívoco de este proyecto en GitHub es:

- **Repositorio:** `RichGutz/Inandes.ERP.React`
- **URL HTTPS:** `https://github.com/RichGutz/Inandes.ERP.React.git`
- **Remoto Git Primario:** `origin`
- **Rama Exclusiva de Trabajo:** `main` (Prohibido crear ramas alternativas, Regla 9)

---

## 2. Procedimiento de Lanzamiento en Entorno Local (Desarrollo)

Para probar la integración completa entre la Interfaz y el Backend, debes tener dos terminales (Powershell) independientes:

**A. Backend (FastAPI - Python)**
- Directorio: `C:\Users\rguti\Inandes.ERP.React\backend`
- Comando: `uvicorn main:app --reload`
- *Nota: Levanta el puerto 8000 por defecto. Aquí conviven las APIs de CRM y Factoring (Originación).*

**B. Frontend (React - Vite)**
- Directorio: `C:\Users\rguti\Inandes.ERP.React`
- Comando: `npm run dev`
- *Nota: Levanta en el puerto local (usualmente 5173). Es el cliente que consume los servicios de Supabase y el Backend FastAPI.*

> **REGLA CRÍTICA PARA AGENTES:** Los agentes NO DEBEN ejecutar `uvicorn` ni `npm run dev / streamlit run` directamente mediante tools, ya que son procesos síncronos continuos que bloquean la sesión. El usuario los lanza manualmente en su terminal.

---

## 3. Conexión con Supabase (SSL Obligatorio)

Toda conexión hacia la base de datos de PostgreSQL administrada por Supabase, ya sea desde Python (SQLAlchemy / psycopg2) o cualquier otro cliente directo de BD, **debe requerir SSL obligatoriamente**.

**Credenciales del Proyecto Supabase Unificado (`egvcinsbyropumybatdf`):**
- `SUPABASE_URL`: `https://egvcinsbyropumybatdf.supabase.co`

**Cadena de Conexión en Backend (Python):**
Asegurarse de que el String termine en `sslmode=require`:
```ini
postgresql://postgres.[PROYECTO]:[PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres?sslmode=require
```

**Conexión en Frontend (React):**
El SDK de Javascript gestiona el SSL automáticamente por debajo usando HTTPS:
```javascript
import { createClient } from '@supabase/supabase-js';
export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY
);
```

---

## 4. Procedimiento Oficial de Despliegue a Producción (VPS Contabo - Coolify)

El entorno de producción oficial se aloja en el servidor VPS Contabo (`169.58.168.107` / Coolify):

### 4.1 Despliegue Único y Automático (CI/CD Git Webhook)
1. **Verificar compilación local:**
   ```powershell
   npm run build
   ```
2. **Guardar y sincronizar en Git (`origin/main`):**
   ```powershell
   git add .
   git commit -m "NOMBRE_DEL_COMMIT"
   git push origin main
   ```
3. **Auto-Deploy por Webhook:**
   Al ejecutar `git push origin main`, el Webhook de Coolify en Contabo recompila y despliega la aplicación de forma automática en caliente (Zero-Downtime Deployment).

> **IMPORTANTE:** El script `deploy_vps.py` y el servidor Hostinger (`91.108.125.253`) han sido dados de baja/desactivados. PROHIBIDO desplegar a Hostinger.

---

## 5. URLs Oficiales en Producción (Contabo)

| Servicio | URL Pública / Endpoint |
|---|---|
| **Frontend Web React** | `https://inandes.geeksoft.tech` |
| **Dashboard Coolify** | `http://169.58.168.107:8000` |
| **Backend FastAPI Factoring** | `https://api-factoring.geeksoft.tech` |
| **Swagger Factoring** | `https://api-factoring.geeksoft.tech/docs` |

---

## 6. Servidores y Ubicación Centralizada de Credenciales

1. **VPS Contabo (Producción Principal Coolify):**
   - Host IP: `169.58.168.107` (Usuario SSH: `root`, puerto 22)
   - Dominio Oficial: `https://inandes.geeksoft.tech`
   - Respaldo de credenciales privadas: `C:\Users\rguti\.gemini\antigravity-ide\scratch\contabo_credentials.json`

2. **VPS Hostinger (DESACTIVADO / OBSOLETO):**
   - IP antigua: `91.108.125.253` — No desplegar.

